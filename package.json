{
  "name": "honeypipe-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "@honeypipe/*",
    "playground"
  ],
  "scripts": {
    "dev": "yarn workspace playground dev",
    "build": "yarn workspace @honeypipe/client build",
    "test": "yarn workspace @honeypipe/client test"
  },
  "devDependencies": {
    "typescript": "^5.5.3"
  }
}